## START: Set by rpmautospec
## (rpmautospec version 0.8.2)
## RPMAUTOSPEC: autorelease, autochangelog
%define autorelease(e:s:pb:n) %{?-p:0.}%{lua:
    release_number = 1;
    base_release_number = tonumber(rpm.expand("%{?-b*}%{!?-b:1}"));
    print(release_number + base_release_number - 1);
}%{?-e:.%{-e*}}%{?-s:.%{-s*}}%{!?-n:%{?dist}}
## END: Set by rpmautospec

# Generated by rust2rpm 25
%bcond_without check

%global crate scx_chaos

Name:           rust-scx_chaos
Version:        1.0.19
Release:        %autorelease
Summary:        A general purpose sched_ext scheduler designed to amplify race conditions for testing and debugging.

License:        GPL-2.0-only
URL:            https://crates.io/crates/scx_chaos
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24
ExcludeArch:    %{ix86}

%global _description %{expand:
scx_chaos is a specialized scheduler that intentionally introduces various forms
of latency and performance degradation to help expose race conditions and timing-dependent bugs
in applications. Unlike traditional schedulers that aim for optimal performance, scx_chaos
is designed to stress-test applications by introducing controlled chaos into the scheduling process.

WARNING: This scheduler is experimental and should not be used in production environments.
It is specifically designed to degrade performance and may cause system instability.
https://github.com/sched-ext/scx/tree/main}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}

License:        GPL-2.0-only AND (MIT OR Apache-2.0) AND Unicode-DFS-2016 AND Apache-2.0 AND (Apache-2.0 OR BSL-1.0) AND (Apache-2.0 WITH LLVM-exception OR Apache-2.0 OR MIT) AND BSD-2-Clause AND BSD-3-Clause AND (BSD-3-Clause OR MIT OR Apache-2.0) AND ISC AND (LGPL-2.1-only OR BSD-2-Clause) AND MIT AND (Unlicense OR MIT) AND Zlib
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%doc README.md
%{_bindir}/scx_chaos

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif
