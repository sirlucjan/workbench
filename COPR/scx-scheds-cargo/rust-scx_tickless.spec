# Generated by rust2rpm 27
%bcond check 1

%global crate scx_tickless

Name:           rust-scx_tickless
Version:        1.0.7
Release:        %autorelease
Summary:        Server-oriented scheduler designed to minimize OS noise and maximize performance isolation

License:        GPL-2.0-only
URL:            https://crates.io/crates/scx_tickless
Source:         %{crates_source}

BuildRequires:  cargo-rpm-macros >= 24

%global _description %{expand:
A server-oriented scheduler designed to minimize OS noise and maximize
performance isolation. https://github.com/sched-ext/scx/tree/main.}

%description %{_description}

%package     -n %{crate}
Summary:        %{summary}
# FIXME: paste output of %%cargo_license_summary here
License:        # FIXME
# LICENSE.dependencies contains a full license breakdown

%description -n %{crate} %{_description}

%files       -n %{crate}
%license LICENSE
%license LICENSE.dependencies
%doc README.md
%{_bindir}/scx_tickless

%prep
%autosetup -n %{crate}-%{version} -p1
%cargo_prep

%generate_buildrequires
%cargo_generate_buildrequires

%build
%cargo_build
%{cargo_license_summary}
%{cargo_license} > LICENSE.dependencies

%install
%cargo_install

%if %{with check}
%check
%cargo_test
%endif

%changelog
%autochangelog
